import React from 'react';
import ReactDOM from 'react-dom';
import { SmartDockApp } from './SmartDockApp';
import './styles/styles.scss';

ReactDOM.render(
  <SmartDockApp />,
  document.getElementById('root')
);
